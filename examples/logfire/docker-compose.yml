services:
  surrealdb:
    image: surrealdb/surrealdb:v2.3.6
    ports:
      - "8000:8000"
    command: start --log trace --user root --pass root --allow-all
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
